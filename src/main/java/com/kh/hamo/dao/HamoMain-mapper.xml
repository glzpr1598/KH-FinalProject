<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">    
<mapper namespace="com.kh.hamo.dao.HamoMainInter">
	<select id="totalClubSearch" resultType="HashMap" >
		SELECT C.club_name, C.club_location,I.Interest_interest FROM club C JOIN interest I ON C.interest_id=I.interest_id WHERE C.club_id LIKE '%'||#{param1}||'%' OR C.club_location LIKE '%'||#{param1}||'%' OR I.interest_interest LIKE '%'||#{param1}||'%';
	</select>
</mapper>